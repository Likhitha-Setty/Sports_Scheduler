<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sports Scheduler</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            font-family: 'Roboto', sans-serif;
            background: url('pexels-markusspiske-114296.jpg') no-repeat center center fixed;
            background-size: cover;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            color: #fff;
        }
        .navbar {
            position: absolute;
            top: 20px;
            right: 20px;
            display: flex;
            gap: 20px;
            font-size: 18px;
            z-index: 1;
            transition: transform 0.3s ease-in-out;
        }
        .navbar a {
            color: #fff;
            text-decoration: none;
            font-weight: bold;
            padding: 10px 20px;
            background-color: rgba(0, 0, 0, 0.6);
            border-radius: 5px;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.4);
            transition: all 0.3s ease;
        }
        .navbar a:hover {
            background-color: rgba(0, 0, 0, 0.8);
            box-shadow: 0px 8px 20px rgba(0, 0, 0, 0.5);
            transform: scale(1.05);
        }
        .container {
            text-align: center;
            background: rgba(0, 0, 0, 0.6);
            padding: 50px 30px;
            border-radius: 15px;
            box-shadow: 0px 6px 15px rgba(0, 0, 0, 0.5);
            transform: translateY(30px);
            animation: fadeIn 1.5s ease-out forwards;
        }
        .heading {
            font-size: 3rem;
            margin-bottom: 20px;
            animation: slideIn 1s ease-out forwards;
        }
        .description {
            font-size: 1.2rem;
            color: #ddd;
            line-height: 1.5;
            opacity: 0;
            animation: fadeInDescription 2s ease-in-out forwards 1.2s;
        }
        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        @keyframes slideIn {
            from {
                transform: translateX(-50px);
                opacity: 0;
            }
            to {
                transform: translateX(0);
                opacity: 1;
            }
        }
        @keyframes fadeInDescription {
            from {
                opacity: 0;
            }
            to {
                opacity: 1;
            }
        }
        @media (max-width: 768px) {
            .navbar {
                top: 10px;
                right: 10px;
                flex-direction: column;
            }
            .heading {
                font-size: 2.5rem;
            }
            .container {
                padding: 40px 20px;
            }
        }
    </style>
</head>
<body>
    <div class="navbar">
        <a href="/register">Register</a>
        <a href="/login">Login</a>
        <a href="/about-us">About Us</a>
    </div>
    <div class="container">
        <h1 class="heading">Sports Scheduler</h1>
        <p class="description">
            Welcome to Sports Scheduler! Our platform allows teams and players to easily organize and schedule sporting events, track match dates, and stay up-to-date with their sports schedules. Get ready for seamless event management and enjoy your game days!
        </p>
    </div>
</body>
</html>
